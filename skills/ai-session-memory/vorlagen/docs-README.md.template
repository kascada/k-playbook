# <Projektname> – Dokumentation

<Ein-Absatz-Beschreibung des Projekts.>

> **Für AI-Sessions:** Diese Docs sind **autoritativ**. Nutze sie zuerst,
> bevor du Code liest. Siehe [`AGENTS.md`](../../AGENTS.md) im Projekt-Root.

Dies ist der einzige Index. Er deckt alle Herkünfte ab; geschrieben wird er von
`/k-docs-index`.

## Übersicht der Dokumente

### Code (`code/`) — aus dem Code abgeleitet

| Datei | Inhalt |
|-------|--------|
| [`code/00-overview.md`](./code/00-overview.md) | Gesamtsystem, Architektur, Datenfluss |
| [`code/01-stack.md`](./code/01-stack.md) | Kompletter Tech-Stack |
| ... | ... |

### Extrahiert (`extracted/`) — erzeugt von `/k-docs-extract`

| Datei | Inhalt | Konfidenz |
|-------|--------|-----------|
| [`extracted/01-<slug>.md`](./extracted/01-<slug>.md) | ... | bestaetigt |

### Versionen (`versions/`) — erzeugt von `/k-doc-inventory`

| Datei | Inhalt | Erhoben |
|-------|--------|---------|
| [`versions/inventory.md`](./versions/inventory.md) | ... | <datum> |

### Handgepflegt (`manual/`)

| Datei | Inhalt |
|-------|--------|
| [`manual/<slug>.md`](./manual/<slug>.md) | ... |

<!-- Abschnitte für Herkünfte ohne Dateien weglassen — eine leere Tabelle ist Rauschen. -->

## Libs & Stack

Kuratierte Referenz zu Libraries und Tools. Fokus: Pitfalls, nicht Tutorials.
Erzeugt von `/k-docs-tools` unter `libs/`.

| Lib | Version | Severity | Letzter Review |
|-----|---------|----------|----------------|
| [<lib>](./libs/<lib>.md) | <version> | <severity> | <datum> |

## Repository-/Modul-Beziehungen

```
<optional: ASCII-Diagramm der Komponenten-Beziehungen>
```

---

## Stichwort-Index

Alphabetisch sortiert. **Format:** Stichwort → Datei + Abschnitt. Jeder Link trägt
das Herkunftsverzeichnis im Pfad.

### A

- **<Begriff>** → `<herkunft>/<datei>.md` §<abschnitt>
- **<Begriff>** → `<herkunft>/<datei>.md` §<abschnitt>

### B

- **<Begriff>** → `<herkunft>/<datei>.md` §<abschnitt>

<!-- ... alle relevanten Buchstaben, mindestens A–Z im Rohbau anlegen -->

---

## Häufige Fragen → Direkter Sprung

| Wenn du das wissen willst … | Schau hier … |
|---|---|
| <Typische Frage 1> | `<herkunft>/<datei>.md` §<abschnitt> |
| <Typische Frage 2> | `<herkunft>/<datei>.md` §<abschnitt> |
| ... | ... |

---

## Kurzfassung des Systems

<Ein paar Absätze zur Zusammenfassung, damit auch ohne Doc-Öffnen ein
Überblick da ist.>

## Workspace-Struktur

```
<projekt-root>/
├── AGENTS.md
├── opencode.json
├── <weitere verzeichnisse>
└── k-playbook-local/
    ├── docs/                  diese Doku
    │   ├── README.md          dieser Index
    │   ├── code/              aus dem Code abgeleitet
    │   ├── libs/              erzeugt von /k-docs-tools
    │   ├── extracted/         erzeugt von /k-docs-extract
    │   ├── versions/          erzeugt von /k-doc-inventory
    │   └── manual/            von Hand gepflegt
    └── material/              Rohmaterial, nie indiziert
```
